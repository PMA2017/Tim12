<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_reservation"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.parkingApp.parkme.activities.ReservationActivity">

    <EditText
        android:id="@+id/dateDrop"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:textAlignment="center"
        android:inputType="datetime"
        android:layout_marginTop="10dp"
        android:hint="@string/dropOffDate" />

    <EditText
        android:id="@+id/dateEnd"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:textAlignment="center"
        android:inputType="datetime"
        android:layout_marginTop="30dp"
        android:hint="@string/dropOffTime"
        android:layout_below="@+id/dateDrop"/>

    <Spinner
        android:id="@+id/spinner1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:drawable/btn_dropdown"
        android:spinnerMode="dropdown"
        android:layout_marginTop="30dp"
        android:layout_below="@+id/dateEnd"/>

    <Button
        android:id="@+id/confirmReservation"
        android:layout_width="250dp"
        android:layout_height="40dp"
        android:layout_below="@id/spinner1"
        android:background="@color/appName"
        android:textStyle="bold"
        android:layout_marginTop="30dp"
        android:layout_centerInParent="true"
        android:text="@string/confirm_reservation"/>


</RelativeLayout>
