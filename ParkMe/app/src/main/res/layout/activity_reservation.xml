<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_reservation"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.parkingApp.parkme.ReservationActivity">

    <TextView
        android:id="@+id/dropOffDate"
        android:layout_height="20dp"
        android:layout_width="200dp"
        android:text="@string/dropOffDate"
        android:layout_alignParentStart="true"
        android:textColor="@color/black"/>

    <TextView
        android:id="@+id/dropOffTime"
        android:layout_height="20dp"
        android:layout_width="match_parent"
        android:text="@string/dropOffTime"
        android:layout_toEndOf="@id/dropOffDate"
        android:textColor="@color/black"/>

    <EditText
        android:id="@+id/dateDrop"
        android:layout_width="100dp"
        android:layout_height="30dp"
        android:layout_marginTop="10dp"
        android:layout_below="@+id/dropOffDate" />


</RelativeLayout>
